# 開発ガイドライン

## 開発コマンド

Node.js で作業する上での基本操作

```bash
# テスト実行
npm run test

# 開発サーバー起動 (ブラウザ上で直に TypeScript + Vue を変換)
npm run dev

# ビルド (dist/ に出力)
npm run build

# ビルド結果のプレビュー (dist/ の内容をローカルサーバーで確認)
npm run preview
```
